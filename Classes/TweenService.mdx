---
title: 'TweenService'
description: '`TweenService` is a class used for tweening objects'
icon: 'arrow-right'
---
<Warning>You cannot Instance this class with `Instance.new()`</Warning>
## Functions
#### `Create`
Returns a Tween. An example:
```lua
local TweenService = Game:GetService("TweenService")
local myTween = TweenService:Create()
```